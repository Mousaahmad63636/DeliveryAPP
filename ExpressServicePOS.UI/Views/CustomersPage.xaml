<!-- File: ExpressServicePOS.UI/Views/CustomersPage.xaml -->

<Page x:Class="ExpressServicePOS.UI.Views.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpressServicePOS.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="إدارة المرسلين">

    <Grid>
        <StackPanel Margin="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="إدارة المرسلين" FontSize="24" FontWeight="Bold"/>
                <Button Grid.Column="1" Content="إضافة مرسل جديد" Width="150" Height="35" Background="#3498db" 
                        Foreground="White" Click="btnAddNewCustomer_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>

            <!-- Rest of the file remains the same -->
        </StackPanel>
    </Grid>
</Page>