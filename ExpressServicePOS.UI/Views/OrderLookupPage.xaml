<Page x:Class="ExpressServicePOS.UI.Views.OrderLookupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="البحث عن طلب">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="البحث عن طلب" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="txtOrderNumber" 
         Grid.Column="0" 
         Margin="0,0,10,0" 
         Height="35"
         Text="أدخل رقم الطلب"/>

            <Button x:Name="btnSearch" 
                    Grid.Column="1" 
                    Content="بحث" 
                    Width="100" 
                    Height="35"
                    Click="btnSearch_Click"/>
        </Grid>

        <Border Grid.Row="2" 
                BorderBrush="#ddd" 
                BorderThickness="1" 
                Padding="15">
            <StackPanel x:Name="orderDetailsPanel" 
                        Visibility="Collapsed">
                <TextBlock Text="تفاصيل الطلب" 
                           FontSize="18" 
                           FontWeight="Bold" 
                           Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="رقم الطلب:" />
                    <TextBlock Grid.Column="1" x:Name="txtDetailsOrderNumber" />

                    <TextBlock Grid.Column="0" Text="اسم العميل:" />
                    <TextBlock Grid.Column="1" x:Name="txtDetailsCustomerName" />

                    <TextBlock Grid.Column="0" Text="العنوان:" />
                    <TextBlock Grid.Column="1" x:Name="txtDetailsCustomerAddress" />

                    <!-- Add more details -->
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>