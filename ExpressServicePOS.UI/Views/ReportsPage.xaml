<Page x:Class="ExpressServicePOS.UI.Views.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpressServicePOS.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="التقارير">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="التقارير" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <Border Background="#f5f5f5" Padding="15" CornerRadius="5" BorderBrush="#ddd" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="كشف حساب عميل" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="اختر العميل:" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" x:Name="cmbCustomers"/>
                    </Grid>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="التاريخ من:" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" x:Name="dpFromDate"/>
                        <TextBlock Grid.Column="2" Text="إلى:" VerticalAlignment="Center" Margin="10,0"/>
                        <DatePicker Grid.Column="3" x:Name="dpToDate"/>
                    </Grid>

                    <Button Content="إنشاء التقرير" Width="150" Height="35" Background="#3498db" Foreground="White" 
                            HorizontalAlignment="Center" Margin="0,10,0,0" Click="btnGenerateCustomerReport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>

            <Border Background="#f5f5f5" Padding="15" CornerRadius="5" BorderBrush="#ddd" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="تقرير المبيعات" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="التاريخ من:" VerticalAlignment="Center"/>
                        <DatePicker Grid.Column="1" x:Name="dpSalesFromDate"/>
                        <TextBlock Grid.Column="2" Text="إلى:" VerticalAlignment="Center" Margin="10,0"/>
                        <DatePicker Grid.Column="3" x:Name="dpSalesToDate"/>
                    </Grid>

                    <Button Content="إنشاء التقرير" Width="150" Height="35" Background="#3498db" Foreground="White" 
                            HorizontalAlignment="Center" Margin="0,10,0,0" Click="btnGenerateSalesReport_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>